services:
  data-gate:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    working_dir: /workspace
    environment:
      GX_ANALYTICS_ENABLED: "false"
      DO_NOT_TRACK: "1"
    volumes:
      - ..:/workspace
    # CPU-only, one-shot
    profiles: []
